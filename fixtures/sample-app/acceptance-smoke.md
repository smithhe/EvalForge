## Feature: Sample App smoke verification

### Acceptance Criteria

- API health endpoint returns 200 on GET /health
- Frontend landing page loads at / with title "Sample App"
- Unit tests pass via `pytest -q`
