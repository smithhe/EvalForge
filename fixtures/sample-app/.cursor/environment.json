{
  "version": "1",
  "install": "pip install -r requirements.txt",
  "terminals": [
    {
      "name": "api",
      "command": "python -m sample_app.server"
    },
    {
      "name": "frontend",
      "command": "python -m http.server 3000 --directory static"
    }
  ]
}
